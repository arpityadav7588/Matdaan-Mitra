{
  "name": "matdaan-mitra",
  "version": "1.0.0",
  "description": "AI-powered election literacy companion for Indian voters",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "npm run dev --workspaces --if-present",
    "build": "npm run build --workspaces --if-present",
    "start": "npm run start --workspaces --if-present"
  },
  "devDependencies": {
    "esbuild": "^0.28.0"
  }
}
